---
import { getCollection, render } from 'astro:content';
import WhitepaperLayout from '../layouts/WhitepaperLayout.astro';
import MindRadar from '../components/MindRadar.astro';

const entries = await getCollection('whitepaper');
const entry = entries[0];
const { Content, headings } = await render(entry);
---

<WhitepaperLayout
  title={entry.data.title}
  description={entry.data.abstract}
  subtitle={entry.data.subtitle}
  authors={entry.data.authors}
  date={entry.data.date}
  version={entry.data.version}
  abstract={entry.data.abstract}
  headings={headings}
>
  <Content components={{ MindRadar }} />
</WhitepaperLayout>
