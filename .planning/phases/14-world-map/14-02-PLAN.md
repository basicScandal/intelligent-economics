---
phase: 14-world-map
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/scripts/dashboard/echarts-setup.ts
  - src/components/dashboard/ScaleTabs.astro
  - src/components/dashboard/DimensionToggle.astro
  - src/pages/dashboard.astro
  - public/geo/world.json
autonomous: true
requirements: [MAP-02, MAP-03]

must_haves:
  truths:
    - "A Map tab button appears in ScaleTabs alongside Country/City/Firm"
    - "Clicking Map tab shows map panel container and hides other scale panels"
    - "DimensionToggle component renders 5 buttons (Composite/M/I/N/D) with role=radiogroup"
    - "ECharts setup registers MapChart, GeoComponent, and VisualMapContinuousComponent"
    - "world.json GeoJSON file exists at public/geo/world.json for lazy fetch"
  artifacts:
    - path: "src/scripts/dashboard/echarts-setup.ts"
      provides: "MapChart + GeoComponent + VisualMapContinuousComponent registration"
      contains: "MapChart"
    - path: "src/components/dashboard/ScaleTabs.astro"
      provides: "4-tab navigation with Map tab"
      contains: "tab-map"
    - path: "src/components/dashboard/DimensionToggle.astro"
      provides: "Segmented button row for dimension switching"
      contains: "dim-toggle"
    - path: "src/pages/dashboard.astro"
      provides: "Map scale panel with map container and dimension toggle"
      contains: "scale-map"
    - path: "public/geo/world.json"
      provides: "GeoJSON world map polygons for ECharts registerMap"
  key_links:
    - from: "src/pages/dashboard.astro"
      to: "src/components/dashboard/DimensionToggle.astro"
      via: "Astro component import"
      pattern: "import DimensionToggle"
    - from: "src/pages/dashboard.astro"
      to: "src/components/dashboard/ScaleTabs.astro"
      via: "Astro component import"
      pattern: "import ScaleTabs"
    - from: "src/scripts/dashboard/echarts-setup.ts"
      to: "echarts/charts"
      via: "MapChart import"
      pattern: "import.*MapChart.*from.*echarts/charts"
---

<objective>
Add the static HTML structure for the world map: ECharts component registration, Map tab in ScaleTabs, DimensionToggle component, map panel in dashboard.astro, and the GeoJSON asset. This plan creates the skeleton -- Plan 03 wires the interactive behavior.

Purpose: Separates static markup from dynamic wiring to keep each plan under context budget.
Output: 5 files -- 2 new (DimensionToggle.astro, public/geo/world.json), 3 modified (echarts-setup.ts, ScaleTabs.astro, dashboard.astro)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-world-map/14-CONTEXT.md
@.planning/phases/14-world-map/14-RESEARCH.md
@.planning/phases/14-world-map/14-UI-SPEC.md
@.planning/phases/14-world-map/14-01-SUMMARY.md
@src/scripts/dashboard/echarts-setup.ts
@src/components/dashboard/ScaleTabs.astro
@src/pages/dashboard.astro

<interfaces>
From Plan 01 outputs:

src/data/geo-name-map.ts:
```typescript
export const GEOJSON_NAME_MAP: Record<string, string>;
```

src/scripts/dashboard/charts.ts (new exports):
```typescript
export type MapDimension = 'mind' | DimensionKey;
export function makeMapOption(countries: SlimCountry[], dimension?: MapDimension): Record<string, unknown>;
```

src/scripts/dashboard/state.ts:
```typescript
export type Scale = 'country' | 'city' | 'firm' | 'map';
```

src/scripts/dashboard/url-state.ts:
```typescript
export interface DashboardURLState {
  primary: string | null;
  compare: string[];
  view?: string | null;
  dim?: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register ECharts map components and fetch GeoJSON asset</name>
  <files>src/scripts/dashboard/echarts-setup.ts, public/geo/world.json</files>
  <read_first>
    - src/scripts/dashboard/echarts-setup.ts (current registration list to extend)
  </read_first>
  <action>
    **Step 1: Extend `src/scripts/dashboard/echarts-setup.ts`**

    Add MapChart to the charts import:
    ```typescript
    import { RadarChart, BarChart, MapChart } from 'echarts/charts';
    ```

    Add GeoComponent and VisualMapContinuousComponent to the components import:
    ```typescript
    import {
      TitleComponent,
      TooltipComponent,
      GridComponent,
      RadarComponent,
      LegendComponent,
      AriaComponent,
      GeoComponent,
      VisualMapContinuousComponent,
    } from 'echarts/components';
    ```

    Add all three to the `echarts.use()` array:
    ```typescript
    echarts.use([
      RadarChart,
      BarChart,
      MapChart,
      TitleComponent,
      TooltipComponent,
      GridComponent,
      RadarComponent,
      LegendComponent,
      AriaComponent,
      GeoComponent,
      VisualMapContinuousComponent,
      SVGRenderer,
    ]);
    ```

    **Step 2: Download world.json to `public/geo/world.json`**

    Fetch the ECharts official world GeoJSON and save it as a static asset:
    ```bash
    mkdir -p public/geo
    curl -o public/geo/world.json https://echarts.apache.org/examples/data/asset/geo/world.json
    ```

    Verify the file is valid JSON and contains features (should be ~987KB).
  </action>
  <verify>
    <automated>grep -q "MapChart" src/scripts/dashboard/echarts-setup.ts && grep -q "VisualMapContinuousComponent" src/scripts/dashboard/echarts-setup.ts && grep -q "GeoComponent" src/scripts/dashboard/echarts-setup.ts && test -f public/geo/world.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/scripts/dashboard/echarts-setup.ts contains `import { RadarChart, BarChart, MapChart }` from echarts/charts
    - src/scripts/dashboard/echarts-setup.ts contains `GeoComponent` in the components import
    - src/scripts/dashboard/echarts-setup.ts contains `VisualMapContinuousComponent` in the components import
    - echarts.use() array contains MapChart, GeoComponent, VisualMapContinuousComponent
    - public/geo/world.json exists and is valid JSON (~987KB)
    - npm run build still succeeds (no import errors)
  </acceptance_criteria>
  <done>ECharts tree-shaken setup includes all three map components, GeoJSON asset available at /geo/world.json</done>
</task>

<task type="auto">
  <name>Task 2: Add Map tab to ScaleTabs and create DimensionToggle component</name>
  <files>src/components/dashboard/ScaleTabs.astro, src/components/dashboard/DimensionToggle.astro</files>
  <read_first>
    - src/components/dashboard/ScaleTabs.astro (existing tab structure to extend with 4th button)
    - .planning/phases/14-world-map/14-UI-SPEC.md (dimension toggle visual spec)
    - .planning/phases/14-world-map/14-CONTEXT.md (D-06 dimension toggle position, D-08 mobile layout)
  </read_first>
  <action>
    **Step 1: Add Map tab to `src/components/dashboard/ScaleTabs.astro`**

    Add a 4th button after the Firm tab, following the exact same pattern as existing tabs:
    ```html
    <button
      role="tab"
      id="tab-map"
      aria-selected="false"
      aria-controls="scale-map"
      data-scale="map"
      tabindex="-1"
      class="inline-flex items-center rounded-full px-4 py-2 text-sm font-medium text-[#8888aa] hover:text-white transition-colors"
    >
      Map
    </button>
    ```

    Place it immediately after the `tab-firm` button, before the closing `</nav>`.

    **Step 2: Create `src/components/dashboard/DimensionToggle.astro`**

    New Astro component following the ScaleTabs pattern. Per D-06: segmented button row, right-aligned. Per UI-SPEC: role="radiogroup", focus-visible ring.

    ```astro
    ---
    // DimensionToggle -- Segmented button row for map dimension switching.
    // Purely presentational; JS in map.ts manages active state.
    ---

    <div id="dim-toggle" class="flex gap-1 bg-surface-2 rounded-full p-1 w-fit ml-auto mb-4" role="radiogroup" aria-label="Map dimension">
      <button role="radio" aria-checked="true" data-dim="mind"
        class="rounded-full px-3 py-1.5 text-sm font-semibold font-body bg-white/10 text-white transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#00c8ff]">
        Composite
      </button>
      <button role="radio" aria-checked="false" data-dim="m"
        class="rounded-full px-3 py-1.5 text-sm font-semibold font-body text-[#8888aa] hover:text-white transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#00c8ff]">
        M
      </button>
      <button role="radio" aria-checked="false" data-dim="i"
        class="rounded-full px-3 py-1.5 text-sm font-semibold font-body text-[#8888aa] hover:text-white transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#00c8ff]">
        I
      </button>
      <button role="radio" aria-checked="false" data-dim="n"
        class="rounded-full px-3 py-1.5 text-sm font-semibold font-body text-[#8888aa] hover:text-white transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#00c8ff]">
        N
      </button>
      <button role="radio" aria-checked="false" data-dim="d"
        class="rounded-full px-3 py-1.5 text-sm font-semibold font-body text-[#8888aa] hover:text-white transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#00c8ff]">
        D
      </button>
    </div>
    ```
  </action>
  <verify>
    <automated>grep -q 'tab-map' src/components/dashboard/ScaleTabs.astro && grep -q 'data-scale="map"' src/components/dashboard/ScaleTabs.astro && grep -q 'dim-toggle' src/components/dashboard/DimensionToggle.astro && grep -q 'role="radiogroup"' src/components/dashboard/DimensionToggle.astro && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/dashboard/ScaleTabs.astro contains button with id="tab-map", data-scale="map", aria-controls="scale-map"
    - src/components/dashboard/ScaleTabs.astro has 4 tab buttons total (Country, City, Firm, Map)
    - src/components/dashboard/DimensionToggle.astro exists with id="dim-toggle" and role="radiogroup"
    - DimensionToggle has exactly 5 buttons with data-dim values: mind, m, i, n, d
    - DimensionToggle buttons have focus-visible:ring-2 ring-[#00c8ff] classes
    - npm run build succeeds
  </acceptance_criteria>
  <done>ScaleTabs has 4 tabs including Map, DimensionToggle component renders 5 dimension buttons with correct ARIA and styling</done>
</task>

<task type="auto">
  <name>Task 3: Add map panel to dashboard.astro page</name>
  <files>src/pages/dashboard.astro</files>
  <read_first>
    - src/pages/dashboard.astro (current page structure with scale panels)
    - src/components/dashboard/DimensionToggle.astro (just created -- import it)
    - .planning/phases/14-world-map/14-UI-SPEC.md (layout contract for desktop and mobile)
    - .planning/phases/14-world-map/14-CONTEXT.md (D-05 map fills main content, D-07 reset button, D-08 mobile)
  </read_first>
  <action>
    **Step 1: Add DimensionToggle import**

    In the frontmatter imports at the top of dashboard.astro, add:
    ```typescript
    import DimensionToggle from '../components/dashboard/DimensionToggle.astro';
    ```

    **Step 2: Add map scale panel**

    After the closing `</div><!-- /scale-firm -->` and before the closing `</div>` of max-w-6xl, add the map panel:

    ```html
    <!-- Map scale panel -->
    <div id="scale-map" data-scale-panel="map" role="tabpanel" aria-labelledby="tab-map" class="hidden">

      <!-- Dimension toggle (right-aligned, per D-06) -->
      <DimensionToggle />

      <!-- Map container -->
      <div class="relative rounded-xl bg-surface border border-border overflow-hidden">
        <!-- Map chart (ECharts renders here) -->
        <div id="map-chart" class="w-full aspect-[2/1] min-h-[400px] max-md:aspect-[4/3] max-md:min-h-[280px]" aria-hidden="true"></div>

        <!-- Loading skeleton (shown until GeoJSON loads) -->
        <div id="map-loading" class="absolute inset-0 flex items-center justify-center bg-surface">
          <div class="text-center">
            <div class="w-8 h-8 border-2 border-white/20 border-t-[#00c8ff] rounded-full animate-spin mx-auto mb-3"></div>
            <p class="text-text-muted text-base">Loading world map...</p>
          </div>
        </div>

        <!-- Error state (hidden by default) -->
        <div id="map-error" class="hidden absolute inset-0 flex items-center justify-center bg-surface">
          <div class="text-center">
            <p class="text-text font-semibold text-base mb-1">Map unavailable</p>
            <p class="text-text-muted text-sm mb-3">Unable to load map data. Check your connection and try again.</p>
            <button id="map-retry" class="px-4 py-2 text-sm font-medium text-[#00c8ff] border border-[#00c8ff]/30 rounded-lg hover:bg-[#00c8ff]/10 transition-colors">Retry loading map</button>
          </div>
        </div>

        <!-- Reset view button (bottom-left, per D-07) -->
        <button id="map-reset-view" class="hidden absolute bottom-3 left-3 px-3 py-1.5 text-xs font-medium text-white/70 bg-surface-2/80 backdrop-blur-sm border border-white/10 rounded-lg hover:text-white hover:bg-surface-2 transition-colors z-10">
          Reset view
        </button>
      </div>

      <!-- Map sr-only text (basic, per INT-01 aria enabled) -->
      <div id="map-chart-sr" class="sr-only" role="status" aria-live="polite">World choropleth map showing MIND scores. Click a country to view details.</div>

      <!-- No country selected prompt -->
      <p id="map-no-selection" class="text-text-muted text-center mt-6 text-sm">
        Click any country on the map to explore its MIND scores
      </p>

      <!-- Country detail after map click (reuses existing pattern, per D-05) -->
      <section id="map-country-detail" class="hidden mt-6">
        <div class="rounded-xl bg-surface border border-border p-6 space-y-6">
          <div id="map-radar-chart" class="w-full h-80 lg:h-96" style="min-height: 320px" aria-hidden="true"></div>
          <div id="map-radar-sr" class="sr-only" role="status" aria-live="polite"></div>
          <div id="map-binding-constraint" class="pl-4 border-l-2 border-white/20">
            <p class="text-sm text-text-muted">Binding constraint: <strong id="map-bc-name" class="text-white"></strong> <span id="map-bc-score" class="text-text-faint"></span></p>
            <p id="map-bc-text" class="text-sm text-text-muted mt-1"></p>
          </div>
          <a id="map-zone-zero-link" href="/intelligent-economics/" class="hidden inline-flex items-center gap-2 px-4 py-2.5 bg-[#00ff88]/10 border border-[#00ff88]/30 rounded-lg text-[#00ff88] hover:bg-[#00ff88]/20 transition-colors text-sm font-medium">
            <span id="map-zone-zero-text">See in Zone Zero simulator</span>
            <span aria-hidden="true">&#8594;</span>
          </a>
          <p class="text-xs text-text-faint">{attribution}</p>
        </div>
      </section>

    </div><!-- /scale-map -->
    ```

    **Key layout details per UI-SPEC:**
    - Desktop: `aspect-[2/1]` with `min-h-[400px]`
    - Mobile (<768px via max-md): `aspect-[4/3]` with `min-h-[280px]`
    - Map container has `relative` for absolute-positioned loading/error/reset overlays
    - Loading state visible by default (map.ts hides it after GeoJSON loads)
    - Error state hidden by default (map.ts shows it on fetch failure)
    - Reset-view button hidden by default (map.ts shows it after zoom/pan)
    - Country detail section hidden until click (map.ts shows it)
  </action>
  <verify>
    <automated>grep -q 'scale-map' src/pages/dashboard.astro && grep -q 'map-chart' src/pages/dashboard.astro && grep -q 'map-loading' src/pages/dashboard.astro && grep -q 'DimensionToggle' src/pages/dashboard.astro && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/dashboard.astro imports DimensionToggle component
    - src/pages/dashboard.astro contains div#scale-map with data-scale-panel="map" and role="tabpanel"
    - src/pages/dashboard.astro contains div#map-chart for ECharts rendering
    - src/pages/dashboard.astro contains div#map-loading with "Loading world map..." text
    - src/pages/dashboard.astro contains div#map-error with "Map unavailable" text and retry button
    - src/pages/dashboard.astro contains button#map-reset-view with "Reset view" text
    - src/pages/dashboard.astro contains section#map-country-detail with radar chart and binding constraint containers
    - Map container uses aspect-[2/1] for desktop and max-md:aspect-[4/3] for mobile
    - npm run build exits 0
  </acceptance_criteria>
  <done>Dashboard page has complete map panel HTML skeleton with loading/error/reset states, dimension toggle, and country detail section, site builds successfully</done>
</task>

</tasks>

<verification>
1. `npm run build` -- site builds without errors
2. `grep -q 'MapChart' src/scripts/dashboard/echarts-setup.ts` -- MapChart registered
3. `grep -q 'tab-map' src/components/dashboard/ScaleTabs.astro` -- Map tab exists
4. `grep -q 'dim-toggle' src/components/dashboard/DimensionToggle.astro` -- DimensionToggle exists
5. `grep -q 'scale-map' src/pages/dashboard.astro` -- Map panel exists
6. `test -f public/geo/world.json` -- GeoJSON asset present
7. `npx vitest run` -- all tests still pass (no regressions)
</verification>

<success_criteria>
- ECharts setup registers MapChart + GeoComponent + VisualMapContinuousComponent
- ScaleTabs has 4 tabs (Country/City/Firm/Map) with correct ARIA attributes
- DimensionToggle has 5 radio buttons (Composite/M/I/N/D) with correct ARIA and styling
- Dashboard page has map panel with chart container, loading skeleton, error state, reset button, country detail
- GeoJSON file exists at public/geo/world.json (~987KB)
- Site builds, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-world-map/14-02-SUMMARY.md`
</output>
