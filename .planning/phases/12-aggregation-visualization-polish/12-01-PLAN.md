---
phase: 12-aggregation-visualization-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/MindScalesPyramid.astro
  - src/components/Nav.astro
  - src/pages/index.astro
autonomous: true
requirements: [AGG-01, NAV-01]

must_haves:
  truths:
    - "A hierarchical visualization on the homepage shows Firm, City, and Country scales with sample MIND scores"
    - "Clicking a pyramid level navigates to the corresponding scale tab on the dashboard"
    - "Whitepaper and Dashboard links appear in the site navigation on every page"
    - "Nav links work on both desktop and mobile viewports"
  artifacts:
    - path: "src/components/MindScalesPyramid.astro"
      provides: "SVG pyramid visualization showing MIND score composition across 3 scales"
      min_lines: 40
    - path: "src/components/Nav.astro"
      provides: "Updated navigation with Whitepaper and Dashboard links"
      contains: "Whitepaper"
    - path: "src/pages/index.astro"
      provides: "Homepage with MindScalesPyramid section between ZoneZeroSimulator and Inversion"
      contains: "MindScalesPyramid"
  key_links:
    - from: "src/components/MindScalesPyramid.astro"
      to: "/dashboard?scale=firm"
      via: "anchor href"
      pattern: "href.*dashboard.*scale="
    - from: "src/components/Nav.astro"
      to: "/whitepaper"
      via: "anchor href"
      pattern: "href.*whitepaper"
    - from: "src/components/Nav.astro"
      to: "/dashboard"
      via: "anchor href"
      pattern: "href.*dashboard"
---

<objective>
Create the hierarchical MIND scales visualization and update site navigation.

Purpose: Show visitors how MIND scores compose across firm, city, and country scales (AGG-01), and make Whitepaper and Dashboard accessible from every page (NAV-01).
Output: MindScalesPyramid.astro component on homepage, updated Nav.astro with two new links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-aggregation-visualization-polish/12-CONTEXT.md
@src/components/Nav.astro
@src/pages/index.astro
@src/components/ZoneZeroSimulator.astro
@src/components/FinalCTA.astro
@src/styles/sections.css

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/mind-score.ts:
```typescript
export type DimensionKey = 'm' | 'i' | 'n' | 'd';
export type DimensionScores = Record<DimensionKey, number>;
export function calcScore(vals: DimensionScores): number;
export function getBindingConstraint(vals: DimensionScores): DimensionKey;
```

Nav.astro link structure (existing pattern):
```html
<a href="#stories" class="nav__link">Stories</a>
<a href="#volunteer" class="nav__link">Volunteer</a>
<!-- New links go here, before the theme toggle button -->
```

Nav mobile breakpoint (from sections.css):
```css
@media (max-width: 600px) {
  .nav__link { display: none; }
}
```

Dashboard scale tab URL pattern (ScaleTabs.astro):
```html
<!-- Tabs use data-scale attribute: "country", "city", "firm" -->
<button role="tab" data-scale="country">Country</button>
```

OKLCH design token colors (from global.css):
- Primary (green): #00ff88
- Secondary (blue): #00c8ff
- Tertiary (purple): #7b4bff
- Accent (gold): #ffb400
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MindScalesPyramid aggregation visualization component</name>
  <files>src/components/MindScalesPyramid.astro</files>
  <read_first>
    - src/components/ZoneZeroSimulator.astro (section structure pattern: section-label, section-title, section-body, reveal class)
    - src/styles/sections.css (existing section patterns, reveal class, OKLCH colors)
    - src/data/city-profiles.json (sample city scores for City level display)
    - src/components/dashboard/ScaleTabs.astro (data-scale attribute values: "country", "city", "firm")
  </read_first>
  <action>
Create `src/components/MindScalesPyramid.astro` — an interactive SVG pyramid/tree diagram per D-AGG-01.

**Section wrapper:** Use the established section pattern from existing components:
```html
<section class="section" id="mind-scales" aria-label="MIND Across Scales">
  <div class="container">
    <div class="reveal">
      <div class="section-label">Aggregation Framework</div>
      <h2 class="section-title">MIND Across Scales</h2>
      <p class="section-body">...</p>
    </div>
    <!-- SVG pyramid here -->
  </div>
</section>
```

**SVG pyramid design (3 levels, bottom-up):**
- **Bottom (Firm):** Widest row. Show icon (building/briefcase emoji or simple SVG icon), "Firm" label, sample score "50". Color: #ffb400 (gold). Link: `/dashboard?scale=firm`
- **Middle (City):** Medium row. Show icon (cityscape), "City" label, sample score "74" (Singapore average). Color: #7b4bff (purple). Link: `/dashboard?scale=city`
- **Top (Country):** Narrowest. Show icon (globe), "Country" label, sample score "56" (Denmark). Color: #00c8ff (blue). Link: `/dashboard?scale=country`

Each level is a clickable `<a>` element wrapping an SVG group. The SVG should be inline (not an external file) for styling control.

**SVG structure:**
- Total viewBox: `0 0 600 400`
- Each level is a rounded rectangle with gradient fill using the dimension accent color at low opacity (10-15%) with a brighter border
- Connecting lines between levels (dashed, white at 20% opacity) showing aggregation flow upward
- Arrow indicators pointing upward between levels
- Text: scale name (bold, white), sample score (accent color, large), brief label (text-muted, small)
- Add `role="img"` and `aria-label="MIND framework aggregation pyramid showing firm, city, and country scales with sample scores"` to the SVG element

**Hover/focus states via CSS:**
- On hover/focus: increase border opacity to 40%, slight scale transform (1.02)
- Use `transition: all 0.2s ease` on the anchor groups
- Focus-visible outline for keyboard navigation

**Responsive:** On mobile (< 640px), the SVG scales down via `max-width: 100%; height: auto`. The viewBox handles scaling naturally.

**Bioluminescent design language:** Use OKLCH accent colors for node borders/highlights. Background of each node: the accent color at 8% opacity. Border: accent color at 25% opacity. Score text: accent color at full brightness.

Do NOT use ECharts or any JS library — pure SVG + CSS per the locked decision.
  </action>
  <verify>
    <automated>cd /Users/scandal/ai/intelligenteconomics && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'mind-scales' src/components/MindScalesPyramid.astro (section ID exists)
    - grep -q 'dashboard.*scale=firm' src/components/MindScalesPyramid.astro (firm link exists)
    - grep -q 'dashboard.*scale=city' src/components/MindScalesPyramid.astro (city link exists)
    - grep -q 'dashboard.*scale=country' src/components/MindScalesPyramid.astro (country link exists)
    - grep -q 'role="img"' src/components/MindScalesPyramid.astro (SVG has role)
    - grep -q 'aria-label' src/components/MindScalesPyramid.astro (SVG has aria-label)
  </acceptance_criteria>
  <done>MindScalesPyramid.astro renders a 3-level SVG pyramid with Firm/City/Country levels, sample MIND scores, clickable links to dashboard scale tabs, and proper ARIA attributes. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Update Nav with Whitepaper/Dashboard links and add pyramid to homepage</name>
  <files>src/components/Nav.astro, src/pages/index.astro</files>
  <read_first>
    - src/components/Nav.astro (current link structure and ordering)
    - src/pages/index.astro (current component ordering — pyramid goes between ZoneZeroSimulator and Inversion)
    - src/styles/sections.css (nav__link styling, mobile breakpoint at 600px)
  </read_first>
  <action>
**Nav.astro changes (per D-NAV-01):**

Add two new `<a>` links inside `.nav__actions`, after the existing "Team" link and before the `<button class="theme-toggle">`:

```html
<a href="/whitepaper" class="nav__link">Whitepaper</a>
<a href="/dashboard" class="nav__link">Dashboard</a>
```

These use the existing `nav__link` class — no new CSS needed. They inherit the existing mobile `display: none` at `max-width: 600px`, matching the behavior of Stories/Volunteer/etc. links.

The link ordering becomes: Stories, Volunteer, Experiments, Partners, Team, Whitepaper, Dashboard, [theme toggle], [Join Now CTA].

**index.astro changes (per D-AGG-01 placement):**

1. Add import at top: `import MindScalesPyramid from '../components/MindScalesPyramid.astro';`
2. Place `<MindScalesPyramid />` between `<ZoneZeroSimulator />` and `<Inversion />` in the main content area.

The section ordering becomes: ... ZoneZeroSimulator, MindScalesPyramid, Inversion, MetabolicRift ...

Do NOT change any other component imports or ordering. Do NOT modify the CTA section or Footer.
  </action>
  <verify>
    <automated>cd /Users/scandal/ai/intelligenteconomics && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'href="/whitepaper"' src/components/Nav.astro (Whitepaper link exists)
    - grep -q 'href="/dashboard"' src/components/Nav.astro (Dashboard link exists)
    - grep -q 'MindScalesPyramid' src/pages/index.astro (component imported and used)
    - grep -q 'nav__link.*Whitepaper\|Whitepaper.*nav__link' src/components/Nav.astro (uses existing link class)
  </acceptance_criteria>
  <done>Nav.astro has Whitepaper and Dashboard links visible on every page using the existing nav__link class. index.astro includes MindScalesPyramid between ZoneZeroSimulator and Inversion. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
1. `npx astro build` completes without errors
2. Built output contains `/whitepaper` and `/dashboard` links in navigation HTML
3. Built homepage contains the `mind-scales` section with SVG pyramid
4. Pyramid links point to `/dashboard?scale=firm`, `/dashboard?scale=city`, `/dashboard?scale=country`
</verification>

<success_criteria>
- Aggregation pyramid SVG renders on homepage between Zone Zero and Inversion sections
- Each pyramid level is clickable and links to the corresponding dashboard scale tab
- Whitepaper and Dashboard appear in Nav.astro using existing nav__link styling
- `npx astro build` succeeds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/12-aggregation-visualization-polish/12-01-SUMMARY.md`
</output>
