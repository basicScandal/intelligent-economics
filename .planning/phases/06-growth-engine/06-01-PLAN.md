---
phase: 06-growth-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Team.astro
  - src/components/VolunteerForm.astro
  - src/pages/index.astro
  - src/components/Nav.astro
  - src/components/Footer.astro
autonomous: true
requirements: [GROW-04, GROW-05, GROW-06, GROW-07]
must_haves:
  truths:
    - "Team/About section shows at least one named person (Rob) with photo placeholder and bio"
    - "Organizational status 'Forming as a nonprofit' is visible in the team section"
    - "Contact email is available in the team section"
    - "Discord invite link placeholder appears on volunteer form success screen"
    - "Team section is accessible via nav link and visible in page flow"
  artifacts:
    - path: "src/components/Team.astro"
      provides: "Team section with founder card, org status, contact info"
      min_lines: 40
    - path: "src/components/VolunteerForm.astro"
      provides: "Updated success state with Discord invite link"
      contains: "discord"
    - path: "src/pages/index.astro"
      provides: "Team component included in page"
      contains: "Team"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/Team.astro"
      via: "Astro component import and placement"
      pattern: "import Team from.*Team.astro"
    - from: "src/components/Nav.astro"
      to: "#team"
      via: "Navigation anchor link"
      pattern: "href.*#team"
---

<objective>
Build the Team/About section and update the volunteer form success screen with a Discord invite link placeholder.

Purpose: Give the movement a human face (GROW-05), make organizational status transparent (GROW-07), provide contact info (GROW-06), and connect new volunteers to Discord community (GROW-04 partial -- the on-site link).

Output: Team.astro component on the live page, updated VolunteerForm.astro success state with Discord CTA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-growth-engine/06-CONTEXT.md

@src/pages/index.astro
@src/components/VolunteerForm.astro
@src/components/Nav.astro
@src/components/Footer.astro
@src/styles/sections.css
@src/content.config.ts

<interfaces>
<!-- Existing patterns the executor needs to follow -->

From src/styles/sections.css — section structure pattern:
```css
.section { padding-block: clamp(var(--space-16), 8vw, var(--space-32)); }
.section-label { /* uppercase green label with left line */ }
.section-title { /* display font, 3xl, bold */ }
.section-body { /* base text, muted color */ }
.container { max-width: var(--content-wide); margin-inline: auto; }
.container--default { max-width: var(--content-default); }
```

From src/components/VolunteerForm.astro — success state (lines 136-145):
```html
<div class="signup-success" id="signup-success" role="alert">
  <div class="success-icon" aria-hidden="true">checkmark</div>
  <h3>You're in the network.</h3>
  <p>The nucleation begins with people exactly like you...</p>
  <div style="margin-top: var(--space-8);">
    <a href="#pillars" class="btn btn--ghost">Explore the Framework</a>
  </div>
</div>
```

From src/pages/index.astro — component order:
VolunteerForm -> Paths -> Partners -> FinalCTA -> Footer

From src/components/Nav.astro — nav link pattern:
```html
<a href="#stories" class="nav__link">Stories</a>
<a href="#volunteer" class="nav__link">Volunteer</a>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Team.astro component with founder card, org status, and contact</name>
  <read_first>
    - src/styles/sections.css (section-label, section-title, section-body patterns)
    - src/components/Partners.astro (similar card-based section for structural reference)
    - src/components/VolunteerForm.astro (existing section structure reference)
  </read_first>
  <files>src/components/Team.astro</files>
  <action>
Create src/components/Team.astro as a new section component matching the site's bioluminescent design language.

**Section structure** (use existing CSS classes where possible):
- Section wrapper: `<section class="team section" id="team">`
- Container: `<div class="container container--default">`
- Intro block: centered section-label "The Team", section-title "Who's Building This", section-body with brief paragraph about the founding stage

**Founder card** (GROW-05):
- Display name: "Rob Ragan"
- Role/title: "Founder"
- Bio: 2-3 sentences about bringing together economics, technology, and systems thinking. Keep it authentic to the movement's tone. Something like: "Security researcher turned economic systems thinker. After two decades breaking systems at Bishop Fox, now focused on designing ones that don't need breaking — starting with how we measure prosperity."
- Avatar: Use an SVG placeholder (a stylized circle with initials "RR" in the site's primary color) — no external image file needed. The user can replace this with a real photo later.
- Contact email: rragan@bishopfox.com displayed as a mailto link (GROW-06)

**Organizational status** (GROW-07):
- Below the founder card, add a status badge/block stating "Currently forming as a nonprofit organization"
- Style it like the existing `.mind-note` pattern: subtle border, dim background, small text

**CSS in component scoped style tag** (not in sections.css):
- `.team__intro` — centered text block
- `.team__card` — frosted glass card matching site aesthetic (rgba background, backdrop-filter blur, border with subtle white)
- `.team__avatar` — 80x80px circle with initials
- `.team__name`, `.team__role`, `.team__bio` — text styles using existing CSS vars
- `.team__contact` — mailto link styled like `.org-card__link` (primary color, small text)
- `.team__status` — status block with subtle green-tinted background

**Responsive:** Card should be max-width ~480px centered on all screens.

Do NOT use content collections for team members — this is a single founder card, not a data-driven list. Keep it simple.
  </action>
  <acceptance_criteria>
    - Team.astro exists with section#team wrapper
    - Founder card shows name "Rob Ragan", role, bio, and contact email
    - Organizational status "Currently forming as a nonprofit organization" is visible
    - Component uses scoped styles matching site aesthetic
    - Card is responsive (centered, max-width constrained)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/scandal/ai/intelligenteconomics && npx astro check 2>&1 | tail -20</automated>
  </verify>
  <done>Team.astro renders a founder card with name, bio, contact email, and nonprofit status — matching site design language</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Team section into page and update form success with Discord link</name>
  <read_first>
    - src/pages/index.astro (current component order)
    - src/components/VolunteerForm.astro (success state div#signup-success)
    - src/components/Nav.astro (nav link list)
    - src/components/Footer.astro (footer nav groups)
  </read_first>
  <files>src/pages/index.astro, src/components/VolunteerForm.astro, src/components/Nav.astro, src/components/Footer.astro</files>
  <action>
**index.astro** — Add Team component to page:
- Import: `import Team from '../components/Team.astro';`
- Place `<Team />` between `<Partners />` and `<FinalCTA />` in the main content flow. This puts the team section near the bottom where "who's behind this" naturally lives, after the user has seen the framework and community info.

**VolunteerForm.astro** — Update success state (GROW-04):
- In the `#signup-success` div, add a Discord invite link AFTER the existing "Explore the Framework" button.
- Add a new link: `<a href="https://discord.gg/PLACEHOLDER" class="btn btn--primary" target="_blank" rel="noopener noreferrer">Join Our Discord</a>`
- Use `btn--primary` class (green, stands out) for the Discord link since this is the key community CTA.
- Add a small text note below: `<p class="success-discord-note">Connect with other volunteers and find your working group</p>`
- Style the note with scoped CSS: small text, muted color, margin-top spacing.
- The href uses "PLACEHOLDER" — the user will replace this with their actual Discord invite link after creating the server (per GROW-03 documentation in Plan 02).

**Nav.astro** — Add Team nav link:
- Add `<a href="#team" class="nav__link">Team</a>` after the "Experiments" link in the nav actions.

**Footer.astro** — Add Team link:
- In the "Act" nav group, add `<li><a href="#team">Team</a></li>` as the last item.
  </action>
  <acceptance_criteria>
    - index.astro imports and renders Team component between Partners and FinalCTA
    - VolunteerForm success state includes Discord invite link with PLACEHOLDER href
    - Nav has "Team" link pointing to #team
    - Footer "Act" group includes Team link
    - Build succeeds with no errors
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/scandal/ai/intelligenteconomics && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>Team section visible in page flow, Discord CTA appears on form success, navigation includes Team link</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. Built HTML at dist/index.html contains `id="team"` section
3. Built HTML contains Discord invite placeholder link in success state
4. Navigation contains `#team` anchor link
</verification>

<success_criteria>
- Team/About section renders on the page with founder info, contact, and nonprofit status
- Volunteer form success screen shows Discord invite CTA
- Team section accessible from nav and footer links
- Site builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/06-growth-engine/06-01-SUMMARY.md`
</output>
