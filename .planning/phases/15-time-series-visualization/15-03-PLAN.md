---
phase: 15-time-series-visualization
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - src/scripts/dashboard/init.ts
  - src/scripts/dashboard/timeline.ts
autonomous: false
requirements: [TIME-06]

must_haves:
  truths:
    - "Users can overlay 2-4 countries on the same time-series chart to compare MIND score trajectories"
    - "Each comparison country line uses a distinct color from COMPARISON_COLORS"
    - "All time-series features work end-to-end: line chart, playback, slider, map recolor, multi-country"
  artifacts:
    - path: "src/scripts/dashboard/timeline.ts"
      provides: "Multi-country overlay rendering using comparison state"
      contains: "COMPARISON_COLORS"
  key_links:
    - from: "src/scripts/dashboard/timeline.ts"
      to: "src/scripts/dashboard/state.ts"
      via: "state.comparison for multi-country overlay"
      pattern: "comparison"
---

<objective>
Ensure multi-country time-series overlay works with comparison state, and perform visual verification of the complete time-series feature set.

Purpose: Validate TIME-06 (multi-country overlay) and confirm all time-series requirements work end-to-end.
Output: Working multi-country comparison on time-series chart + human verification of entire feature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-time-series-visualization/15-CONTEXT.md
@.planning/phases/15-time-series-visualization/15-UI-SPEC.md
@.planning/phases/15-time-series-visualization/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix multi-country time-series overlay</name>
  <files>
    src/scripts/dashboard/timeline.ts,
    src/scripts/dashboard/init.ts
  </files>
  <read_first>
    src/scripts/dashboard/timeline.ts,
    src/scripts/dashboard/init.ts,
    src/scripts/dashboard/charts.ts,
    src/scripts/dashboard/state.ts,
    .planning/phases/15-time-series-visualization/15-02-SUMMARY.md
  </read_first>
  <action>
    Verify that timeline.ts store subscription already handles comparison countries (per D-03):

    In the store.subscribe callback inside timeline.ts, the logic should already:
    1. Build a TimeSeriesCountry for the primary country with color '#00c8ff'
    2. For each comparison country (state.comparison, excluding primary by code), build a TimeSeriesCountry using `COMPARISON_COLORS[idx % COMPARISON_COLORS.length]`
    3. Pass the full array to makeTimeSeriesOption
    4. Update the legend to show country names

    If this is NOT already correctly implemented in Plan 02's timeline.ts, fix it:
    - Filter `state.comparison` to exclude primary: `const others = state.comparison.filter(c => c.code !== state.primary?.code);`
    - Build TimeSeriesCountry for each, using getCountryTimeSeries for historical data
    - Combine: `[primaryTs, ...otherTs]`
    - Pass to makeTimeSeriesOption

    Also verify that when comparison changes (add/remove country), the line chart re-renders with updated overlay lines.

    Run the build and tests to confirm no regressions.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "COMPARISON_COLORS" src/scripts/dashboard/timeline.ts returns 0
    - grep -q "comparison" src/scripts/dashboard/timeline.ts returns 0
    - grep -q "getCountryTimeSeries" src/scripts/dashboard/timeline.ts returns 0
    - npm run build exits 0
    - npx vitest run exits 0
  </acceptance_criteria>
  <done>
    Multi-country overlay renders comparison countries with distinct COMPARISON_COLORS on the time-series chart. Adding or removing comparison countries updates the chart immediately.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete time-series feature</name>
  <files>n/a</files>
  <action>
    Human visual verification of the complete time-series feature set covering all TIME-02 through TIME-06 requirements.
  </action>
  <what-built>
    Full time-series visualization for the MIND dashboard:
    1. Line chart showing MIND score evolution (2014-2024) for selected country
    2. Play/pause animation with 3 speed settings (1x, 2x, 0.5x)
    3. Year slider for manual scrubbing
    4. Map recoloring when a historical year is selected
    5. Multi-country overlay (add 2-4 countries and see overlaid lines)
    6. Year indicator bar with "Back to latest" button
    7. URL state includes year parameter
  </what-built>
  <how-to-verify>
    1. Visit http://localhost:4321/intelligent-economics/dashboard
    2. Search for "Norway" and select it
    3. Verify: a line chart titled "MIND Score Evolution" appears below the radar chart
    4. Verify: the chart shows lines from 2014 to 2024
    5. Click the play button (triangle icon left of the slider)
    6. Verify: years animate forward, slider thumb moves, year updates
    7. Click the speed badge to cycle through 1x/2x/0.5x
    8. Drag the year slider to 2018
    9. Verify: radar chart updates to 2018 data, year bar shows "Viewing: 2018"
    10. Click the Map tab
    11. Verify: map shows 2018 data coloring (not latest year)
    12. Click "Back to latest" in the year bar
    13. Verify: map reverts to current year coloring, year bar disappears
    14. Go back to Country tab, search and add "Sweden" via "+ Compare" button
    15. Verify: time-series chart now shows 2 overlaid lines in different colors
    16. Add "Finland" and "Denmark" to comparison
    17. Verify: 4 colored lines visible on the chart with legend showing country names
    18. Check the URL bar -- it should include year and country params
    19. Copy the URL, open in new tab -- verify same state is restored
    20. Switch to City tab -- verify year resets to latest
  </how-to-verify>
  <verify>
    <automated>npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Human confirms all 20 verification steps pass. All TIME-02 through TIME-06 requirements verified visually.</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- All TIME-02 through TIME-06 requirements verified visually
- `npm run build` clean
- `npx vitest run` all tests pass
- URL state round-trip works for year parameter
</verification>

<success_criteria>
- Multi-country overlay shows 2-4 countries with distinct colors on time-series chart
- Full playback cycle works (play, pause, speed change, slider scrub)
- Map recolors for historical years
- Year bar appears/disappears correctly
- Human approves visual quality of the complete feature
</success_criteria>

<output>
After completion, create `.planning/phases/15-time-series-visualization/15-03-SUMMARY.md`
</output>
