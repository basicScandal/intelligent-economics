---
phase: 11-multi-scale-cross-linking
plan: 03
type: execute
wave: 2
depends_on: [11-02]
files_modified:
  - src/components/dashboard/FirmAssessment.astro
  - src/pages/dashboard.astro
  - src/scripts/dashboard/init.ts
autonomous: true
requirements: [SCALE-02]

must_haves:
  truths:
    - "Users can move 4 sliders (M/I/N/D, 0-100) on the Firm tab and see a live MIND score update"
    - "The firm assessment shows a binding constraint callout identifying the weakest dimension"
    - "The firm assessment reuses the same calcScore() and getBindingConstraint() functions from the shared MIND library"
    - "The Firm tab content renders server-side and sliders work with progressive enhancement"
  artifacts:
    - path: "src/components/dashboard/FirmAssessment.astro"
      provides: "Static HTML structure for firm self-assessment with 4 slider inputs"
      contains: "input type"
    - path: "src/pages/dashboard.astro"
      provides: "Dashboard page with firm assessment content in the firm scale panel"
      contains: "FirmAssessment"
    - path: "src/scripts/dashboard/init.ts"
      provides: "Firm slider input event wiring with live score calculation"
      contains: "firm"
  key_links:
    - from: "src/scripts/dashboard/init.ts"
      to: "src/lib/mind-score.ts"
      via: "dynamic import for calcScore and getBindingConstraint"
      pattern: "calcScore\\|getBindingConstraint"
    - from: "src/pages/dashboard.astro"
      to: "src/components/dashboard/FirmAssessment.astro"
      via: "Astro component import"
      pattern: "FirmAssessment"
---

<objective>
Implement the firm-level MIND self-assessment tool on the dashboard Firm tab, allowing users to adjust 4 dimension sliders and see a live MIND score with binding constraint feedback.

Purpose: The firm self-assessment makes the MIND framework tangible and personal -- visitors can evaluate their own organization against the same framework used to analyze countries and cities. This is a key conversion tool: users who assess their own firm are deeply engaged with the MIND concept.

Output: FirmAssessment.astro static component with slider inputs, dashboard.astro updated with firm content, init.ts extended with slider event wiring and live score calculation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-multi-scale-cross-linking/11-CONTEXT.md
@.planning/phases/11-multi-scale-cross-linking/11-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/mind-score.ts:
```typescript
export type DimensionKey = 'm' | 'i' | 'n' | 'd';
export type DimensionScores = Record<DimensionKey, number>;
export function calcScore(vals: DimensionScores): number;
export function getBindingConstraint(vals: DimensionScores): DimensionKey;
```

From src/scripts/zone-zero.ts (pattern reference for slider UX):
```typescript
// DOM pattern: range input per dimension with value display and warning elements
// IDs: zz-m, zz-i, zz-n, zz-d (sliders), zz-m-val etc (value display)
// COLORS: m='#00ff88', i='#00c8ff', n='#7b4bff', d='#ffb400'
// FLOOR = 15 (below this triggers warning)
// Uses calcScore() from shared library for score computation
```

From src/scripts/dashboard/charts.ts:
```typescript
export const DIM_COLORS: Record<DimensionKey, string> = {
  m: '#00ff88', i: '#00c8ff', n: '#7b4bff', d: '#ffb400',
};
export const DIM_NAMES: Record<DimensionKey, string> = {
  m: 'Material', i: 'Intelligence', n: 'Network', d: 'Diversity',
};
export function getBindingConstraintCallout(dimKey: DimensionKey, score: number): BindingConstraintCallout;
```

Dashboard Firm panel placeholder (from Plan 02):
```html
<div id="scale-firm" data-scale-panel="firm" class="hidden">
  <!-- Firm self-assessment content added by Plan 03 -->
</div>
```

State store (from Plan 02):
```typescript
export type Scale = 'country' | 'city' | 'firm';
export interface DashboardState {
  primary: SlimCountry | null;
  comparison: SlimCountry[];
  isMobile: boolean;
  activeScale: Scale;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FirmAssessment.astro component with slider inputs and score display</name>
  <files>src/components/dashboard/FirmAssessment.astro</files>
  <read_first>
    - src/scripts/zone-zero.ts (lines 1-120 — slider DOM pattern, COLORS, DEFAULTS, FLOOR constants)
    - src/components/dashboard/BindingConstraint.astro (pattern reference for constraint callout DOM)
    - src/scripts/dashboard/charts.ts (DIM_COLORS, DIM_NAMES constants)
  </read_first>
  <action>
Create `src/components/dashboard/FirmAssessment.astro` — a static Astro component rendering the firm self-assessment UI. Per user decision: "Reuse Zone Zero slider pattern — 4 sliders (M/I/N/D, 0-100) with live MIND score and binding constraint callout. Uses existing calcScore() and getBindingConstraint(). No saved state."

**Structure:**

```html
<div class="max-w-2xl mx-auto">
  <h2 class="text-2xl font-display font-semibold text-text mb-2 text-center">Firm MIND Self-Assessment</h2>
  <p class="text-text-muted text-center mb-8 max-w-xl mx-auto">
    Rate your organization across MIND's four dimensions. Move the sliders to see your firm's MIND score and identify your binding constraint.
  </p>

  <!-- Score display -->
  <div class="text-center mb-8">
    <div class="text-6xl font-display font-bold text-white" id="firm-score">50</div>
    <div class="text-sm text-text-muted mt-1">MIND Score</div>
  </div>

  <!-- Slider section -->
  <div class="space-y-6" id="firm-sliders">
    <!-- Material slider -->
    <div class="firm-slider-group">
      <div class="flex justify-between items-center mb-2">
        <label for="firm-m" class="text-sm font-medium" style="color: #00ff88;">Material</label>
        <span id="firm-m-val" class="text-sm text-white font-mono">50</span>
      </div>
      <input type="range" id="firm-m" min="0" max="100" value="50"
             class="w-full h-2 rounded-full appearance-none cursor-pointer"
             style="accent-color: #00ff88;"
             aria-label="Material dimension score" />
      <p class="text-xs text-text-faint mt-1">Physical infrastructure, revenue, assets, and operational capacity</p>
    </div>

    <!-- Intelligence slider -->
    <div class="firm-slider-group">
      <div class="flex justify-between items-center mb-2">
        <label for="firm-i" class="text-sm font-medium" style="color: #00c8ff;">Intelligence</label>
        <span id="firm-i-val" class="text-sm text-white font-mono">50</span>
      </div>
      <input type="range" id="firm-i" min="0" max="100" value="50"
             class="w-full h-2 rounded-full appearance-none cursor-pointer"
             style="accent-color: #00c8ff;"
             aria-label="Intelligence dimension score" />
      <p class="text-xs text-text-faint mt-1">R&D investment, employee skills, knowledge management, and innovation</p>
    </div>

    <!-- Network slider -->
    <div class="firm-slider-group">
      <div class="flex justify-between items-center mb-2">
        <label for="firm-n" class="text-sm font-medium" style="color: #7b4bff;">Network</label>
        <span id="firm-n-val" class="text-sm text-white font-mono">50</span>
      </div>
      <input type="range" id="firm-n" min="0" max="100" value="50"
             class="w-full h-2 rounded-full appearance-none cursor-pointer"
             style="accent-color: #7b4bff;"
             aria-label="Network dimension score" />
      <p class="text-xs text-text-faint mt-1">Partnerships, market reach, supply chain diversity, and customer base</p>
    </div>

    <!-- Diversity slider -->
    <div class="firm-slider-group">
      <div class="flex justify-between items-center mb-2">
        <label for="firm-d" class="text-sm font-medium" style="color: #ffb400;">Diversity</label>
        <span id="firm-d-val" class="text-sm text-white font-mono">50</span>
      </div>
      <input type="range" id="firm-d" min="0" max="100" value="50"
             class="w-full h-2 rounded-full appearance-none cursor-pointer"
             style="accent-color: #ffb400;"
             aria-label="Diversity dimension score" />
      <p class="text-xs text-text-faint mt-1">Team diversity, product portfolio breadth, revenue source variety, and resilience</p>
    </div>
  </div>

  <!-- Binding constraint callout -->
  <div id="firm-bc" class="mt-8 pl-4 border-l-2 rounded-r-lg bg-white/5 py-3 pr-4" style="border-left-color: #ffb400;">
    <p class="text-sm font-medium text-white">
      Binding constraint: <strong id="firm-bc-name">Diversity</strong>
      <span id="firm-bc-score" class="text-text-faint">(50)</span>
    </p>
    <p id="firm-bc-text" class="text-sm text-text-muted mt-1">
      Diversity (50) is the binding constraint. Monocultures are fragile. Variety is the system's immune response to shocks.
    </p>
  </div>

  <!-- Whitepaper CTA -->
  <div class="mt-8 text-center">
    <a href="/whitepaper#mathematical-foundation" class="text-[#00ff88] hover:underline text-sm">
      Learn how the MIND formula works &rarr;
    </a>
  </div>

  <!-- No-JS fallback -->
  <noscript>
    <p class="text-sm text-text-muted mt-4 text-center">
      Enable JavaScript to use the interactive self-assessment tool.
    </p>
  </noscript>
</div>
```

**Key design decisions (Claude's discretion):**
- Stacked slider layout (not side-by-side) — works on all screen sizes, matches Zone Zero vertical flow
- Default all sliders to 50 — neutral starting point, invites exploration
- Brief description under each slider explaining what that dimension means for a firm
- Initial binding constraint shows whichever dimension is lowest (at equal values, first in m/i/n/d order per getBindingConstraint logic = 'm')
- Link to whitepaper mathematical foundation section for methodology context
  </action>
  <verify>
    <automated>cd /Users/scandal/ai/intelligenteconomics && npx astro check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'id="firm-score"' src/components/dashboard/FirmAssessment.astro (score display element)
    - grep -q 'id="firm-m"' src/components/dashboard/FirmAssessment.astro (Material slider)
    - grep -q 'id="firm-i"' src/components/dashboard/FirmAssessment.astro (Intelligence slider)
    - grep -q 'id="firm-n"' src/components/dashboard/FirmAssessment.astro (Network slider)
    - grep -q 'id="firm-d"' src/components/dashboard/FirmAssessment.astro (Diversity slider)
    - grep -q 'id="firm-bc"' src/components/dashboard/FirmAssessment.astro (binding constraint container)
    - grep -q 'id="firm-bc-name"' src/components/dashboard/FirmAssessment.astro (BC dimension name)
    - grep -q 'type="range"' src/components/dashboard/FirmAssessment.astro (range inputs)
    - grep -q '/whitepaper' src/components/dashboard/FirmAssessment.astro (cross-link)
  </acceptance_criteria>
  <done>FirmAssessment.astro renders 4 dimension sliders (0-100), a MIND score display, and a binding constraint callout with proper DOM IDs for JavaScript wiring. Includes dimension descriptions and whitepaper cross-link.</done>
</task>

<task type="auto">
  <name>Task 2: Wire FirmAssessment into dashboard page and add slider event handling in init.ts</name>
  <files>src/pages/dashboard.astro, src/scripts/dashboard/init.ts</files>
  <read_first>
    - src/pages/dashboard.astro (full file — find #scale-firm placeholder from Plan 02)
    - src/scripts/dashboard/init.ts (full file — understand where to add firm logic)
    - src/components/dashboard/FirmAssessment.astro (created in Task 1 — confirm DOM IDs)
    - src/lib/mind-score.ts (calcScore, getBindingConstraint signatures)
    - src/scripts/dashboard/charts.ts (getBindingConstraintCallout, DIM_COLORS)
  </read_first>
  <action>
**Step A: Import and render FirmAssessment in dashboard.astro.**

1. Add import in frontmatter: `import FirmAssessment from '../components/dashboard/FirmAssessment.astro';`
2. Replace the `<!-- Firm self-assessment content added by Plan 03 -->` comment inside `#scale-firm` with: `<FirmAssessment />`

**Step B: Add firm slider wiring in init.ts.**

Add a new section 7 to init.ts, AFTER the existing sections. This section should be eagerly loaded (no ECharts dependency — firm assessment uses no charts, just calcScore which is lightweight):

```typescript
// ── 7. Firm self-assessment slider wiring (eager — lightweight imports only) ──

const firmSliders = {
  m: document.getElementById('firm-m') as HTMLInputElement | null,
  i: document.getElementById('firm-i') as HTMLInputElement | null,
  n: document.getElementById('firm-n') as HTMLInputElement | null,
  d: document.getElementById('firm-d') as HTMLInputElement | null,
};
const firmScoreEl = document.getElementById('firm-score');
const firmValEls = {
  m: document.getElementById('firm-m-val'),
  i: document.getElementById('firm-i-val'),
  n: document.getElementById('firm-n-val'),
  d: document.getElementById('firm-d-val'),
};
const firmBcName = document.getElementById('firm-bc-name');
const firmBcScore = document.getElementById('firm-bc-score');
const firmBcText = document.getElementById('firm-bc-text');
const firmBcEl = document.getElementById('firm-bc');

// Only wire if firm sliders exist (they're in a hidden panel, so check)
if (firmSliders.m && firmSliders.i && firmSliders.n && firmSliders.d) {
  // Import calcScore eagerly — it's tiny (no heavy deps)
  import('../../lib/mind-score').then(({ calcScore, getBindingConstraint }) => {
    // Import chart utilities for binding constraint callout text
    import('../dashboard/charts').then(({ getBindingConstraintCallout, DIM_COLORS }) => {
      
      function updateFirmScore() {
        const vals = {
          m: Number(firmSliders.m!.value),
          i: Number(firmSliders.i!.value),
          n: Number(firmSliders.n!.value),
          d: Number(firmSliders.d!.value),
        };

        // Update value displays
        if (firmValEls.m) firmValEls.m.textContent = String(vals.m);
        if (firmValEls.i) firmValEls.i.textContent = String(vals.i);
        if (firmValEls.n) firmValEls.n.textContent = String(vals.n);
        if (firmValEls.d) firmValEls.d.textContent = String(vals.d);

        // Compute MIND score
        const score = calcScore(vals);
        if (firmScoreEl) firmScoreEl.textContent = String(score);

        // Binding constraint
        const bcKey = getBindingConstraint(vals);
        const bcScore = vals[bcKey];
        const bc = getBindingConstraintCallout(bcKey, bcScore);
        if (firmBcName) firmBcName.textContent = bc.dimension;
        if (firmBcScore) firmBcScore.textContent = `(${bcScore})`;
        if (firmBcText) firmBcText.textContent = bc.text;
        if (firmBcEl) firmBcEl.style.borderLeftColor = DIM_COLORS[bcKey];
      }

      // Attach input listeners
      (['m', 'i', 'n', 'd'] as const).forEach(dim => {
        firmSliders[dim]!.addEventListener('input', updateFirmScore);
      });

      // Initial calculation (sliders start at 50)
      updateFirmScore();
    });
  });
}
```

**Key implementation notes:**
- Uses dynamic `import()` for mind-score.ts and charts.ts — these are lightweight but keeping the pattern consistent with the rest of init.ts
- No ECharts needed — the firm assessment is pure DOM manipulation
- The `updateFirmScore` function runs on every slider input event for instant feedback
- `getBindingConstraint` from the shared library determines which dimension is lowest
- `getBindingConstraintCallout` provides the human-readable insight text
- All DOM IDs match exactly what FirmAssessment.astro defines
- No saved state per user decision ("No saved state")
  </action>
  <verify>
    <automated>cd /Users/scandal/ai/intelligenteconomics && npm run build 2>&1 | tail -20 && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "FirmAssessment" src/pages/dashboard.astro (component imported and rendered)
    - grep -q "firm-m" src/scripts/dashboard/init.ts (firm slider wiring)
    - grep -q "firm-score" src/scripts/dashboard/init.ts (score display update)
    - grep -q "calcScore" src/scripts/dashboard/init.ts (shared library reuse)
    - grep -q "getBindingConstraint" src/scripts/dashboard/init.ts (shared library reuse)
    - grep -q "updateFirmScore" src/scripts/dashboard/init.ts (update function exists)
    - npm run build succeeds
    - npx vitest run passes (existing tests unbroken)
  </acceptance_criteria>
  <done>FirmAssessment component rendered in the dashboard Firm tab. Slider input events trigger live MIND score calculation via calcScore() and binding constraint identification via getBindingConstraint() from the shared library. All 4 sliders update the score display and constraint callout instantly. Site builds and all tests pass.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npx vitest run` — all existing tests pass
3. Dashboard HTML contains firm slider inputs with proper IDs
4. Built dashboard JS contains firm slider event wiring and calcScore import
5. Firm tab shows 4 sliders, a MIND score, and a binding constraint callout
</verification>

<success_criteria>
- Users can switch to the Firm tab and see 4 dimension sliders starting at 50
- Moving any slider instantly updates the MIND score display
- The binding constraint callout updates to show the weakest dimension with insight text
- Setting any slider to 0 drives the MIND score to 0 (zero-floor property works)
- The firm assessment uses calcScore() and getBindingConstraint() from src/lib/mind-score.ts (shared library, not duplicated logic)
- No additional heavy dependencies loaded for the firm tab (no ECharts)
- Site builds and all existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-multi-scale-cross-linking/11-03-SUMMARY.md`
</output>
