---
phase: 01-foundation-and-deploy
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - netlify.toml
  - .github/workflows/deploy.yml
  - .github/workflows/static.yml
  - docs/email-dns-setup.md
autonomous: false
requirements:
  - ARCH-04
must_haves:
  truths:
    - "Pushing to main branch triggers a Netlify deploy that completes successfully"
    - "The deployed site at the Netlify URL shows the Astro placeholder page (not the old index.html)"
    - "GitHub Pages workflows no longer run on push to main"
  artifacts:
    - path: "netlify.toml"
      provides: "Netlify build configuration"
      contains: 'publish = "dist"'
    - path: ".github/workflows/deploy.yml"
      provides: "Disabled GitHub Pages workflow"
      contains: "workflow_dispatch"
    - path: ".github/workflows/static.yml"
      provides: "Disabled GitHub Pages workflow"
      contains: "workflow_dispatch"
    - path: "docs/email-dns-setup.md"
      provides: "Email DNS setup documentation"
      contains: "v=spf1"
  key_links:
    - from: "netlify.toml"
      to: "package.json"
      via: "build command references npm run build"
      pattern: 'command = "npm run build"'
    - from: "netlify.toml"
      to: "dist/"
      via: "publish directory"
      pattern: 'publish = "dist"'
---

<objective>
Configure Netlify CI/CD pipeline with automatic deploys from main branch and PR preview deploys. Disable competing GitHub Pages workflows. Document email DNS records for domain warming.

Purpose: Complete the deployment infrastructure (ARCH-04) so every push to main produces a live site and every PR gets a preview URL. Email DNS documentation starts the 4-6 week domain warming clock needed before Phase 6 sends actual emails.

Output: Working Netlify deploy pipeline, disabled GitHub Pages workflows, email DNS record documentation for user to apply.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-deploy/01-RESEARCH.md
@.planning/phases/01-foundation-and-deploy/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs needed by this plan -->
From package.json:
- "build" script runs `astro build`
- Build output goes to `dist/`

From astro.config.mjs:
- Static output mode (default, no adapter)
- Site URL: https://intelligenteconomics.ai
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root netlify.toml and disable GitHub Pages workflows</name>
  <files>netlify.toml, .github/workflows/deploy.yml, .github/workflows/static.yml</files>
  <read_first>
    - .netlify/netlify.toml (existing auto-generated Netlify config — publishes from repo root, must be superseded by new root netlify.toml)
    - .github/workflows/deploy.yml (current GitHub Pages workflow — must be disabled)
    - .github/workflows/static.yml (current GitHub Pages workflow — must be disabled)
    - .github/workflows/manual.yml (keep this one unchanged — it is manual-trigger only)
    - package.json (verify "build" script exists as "astro build")
  </read_first>
  <action>
**Create netlify.toml at project root** (NOT inside .netlify/):

```toml
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Redirect old privacy.html to new Astro route
[[redirects]]
  from = "/privacy.html"
  to = "/privacy/"
  status = 301

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
```

**Disable .github/workflows/deploy.yml** — Remove the `push: branches: [main]` trigger so it no longer auto-runs. Keep only `workflow_dispatch` for manual use if ever needed. Replace the file content with:

```yaml
name: Deploy to GitHub Pages (DISABLED)
# Disabled — site now deploys via Netlify
# Kept for reference. Remove entirely after Netlify migration confirmed.

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

**Disable .github/workflows/static.yml** — Same approach. Remove push trigger, keep only workflow_dispatch:

```yaml
name: Deploy static content to Pages (DISABLED)
# Disabled — site now deploys via Netlify
# Kept for reference. Remove entirely after Netlify migration confirmed.

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
```

**Do NOT modify .github/workflows/manual.yml** — it is already manual-trigger only.

**Do NOT delete the .netlify/ directory** — it contains state.json with the Netlify site ID. It is already in .gitignore (added in Plan 01). The root netlify.toml takes precedence for Git-based deploys.
  </action>
  <verify>
    <automated>cd /Users/scandal/ai/intelligenteconomics && test -f netlify.toml && grep -q 'publish = "dist"' netlify.toml && grep -q 'NODE_VERSION = "22"' netlify.toml && grep -q 'command = "npm run build"' netlify.toml && echo "NETLIFY_TOML_OK" && grep -q 'workflow_dispatch' .github/workflows/deploy.yml && ! grep -q 'push:' .github/workflows/deploy.yml && echo "DEPLOY_YML_DISABLED" && grep -q 'workflow_dispatch' .github/workflows/static.yml && ! grep -q 'push:' .github/workflows/static.yml && echo "STATIC_YML_DISABLED"</automated>
  </verify>
  <acceptance_criteria>
    - netlify.toml exists at project root (NOT inside .netlify/)
    - netlify.toml contains `command = "npm run build"`
    - netlify.toml contains `publish = "dist"`
    - netlify.toml contains `NODE_VERSION = "22"`
    - .github/workflows/deploy.yml does NOT contain `push:` trigger
    - .github/workflows/deploy.yml contains `workflow_dispatch` as only trigger
    - .github/workflows/deploy.yml contains "DISABLED" in name
    - .github/workflows/static.yml does NOT contain `push:` trigger
    - .github/workflows/static.yml contains `workflow_dispatch` as only trigger
    - .github/workflows/static.yml contains "DISABLED" in name
    - .github/workflows/manual.yml is UNCHANGED
  </acceptance_criteria>
  <done>Root netlify.toml configures Astro build for Netlify, both GitHub Pages auto-deploy workflows are disabled (manual-only), manual.yml is preserved unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Create email DNS records documentation for domain warming</name>
  <files>docs/email-dns-setup.md</files>
  <read_first>
    - .planning/phases/01-foundation-and-deploy/01-RESEARCH.md (Email DNS Records section with SPF/DKIM/DMARC values)
  </read_first>
  <action>
Create `docs/email-dns-setup.md` with the exact DNS records the user needs to add to their domain registrar. This documentation enables the user to configure SPF/DKIM/DMARC for domain warming before Phase 6 sends actual emails.

Contents:

```markdown
# Email DNS Setup — intelligenteconomics.ai

## Why Now

Email deliverability requires domain authentication. SPF, DKIM, and DMARC records
need 4-6 weeks of "warming" before sending marketing emails in Phase 6. Configure
these records as soon as possible.

## Records to Add

Add these DNS records at your domain registrar (wherever intelligenteconomics.ai DNS is managed).

### 1. SPF Record

| Type | Host | Value |
|------|------|-------|
| TXT | @ | `v=spf1 include:mlsend.com ~all` |

Allows MailerLite (mlsend.com) to send email on behalf of the domain.

### 2. DKIM Record

| Type | Host | Value |
|------|------|-------|
| CNAME | ml._domainkey | `ml.domainkey.mlsend.com` |

NOTE: The exact CNAME value may differ. After adding intelligenteconomics.ai
to MailerLite (Settings > Domains > Add domain), MailerLite will show the
exact DKIM CNAME record to create. Use those values instead if they differ.

### 3. DMARC Record

| Type | Host | Value |
|------|------|-------|
| TXT | _dmarc | `v=DMARC1; p=none; rua=mailto:dmarc@intelligenteconomics.ai; pct=100` |

Starts in monitoring mode (p=none). Can be tightened to p=quarantine after
verifying deliverability in Phase 6.

## Verification

After adding records, verify propagation:

1. SPF: https://mxtoolbox.com/spf.aspx — enter intelligenteconomics.ai
2. DKIM: https://mxtoolbox.com/dkim.aspx — selector: ml, domain: intelligenteconomics.ai
3. DMARC: https://mxtoolbox.com/dmarc.aspx — enter intelligenteconomics.ai

DNS propagation can take up to 48 hours.

## MailerLite Account Setup

1. Create account at https://www.mailerlite.com/ (free tier: 1000 subscribers)
2. Go to Settings > Domains > Add domain
3. Enter intelligenteconomics.ai
4. MailerLite will provide exact DNS records — cross-reference with above
5. Click "Verify" after DNS records propagate
```
  </action>
  <verify>
    <automated>cd /Users/scandal/ai/intelligenteconomics && test -f docs/email-dns-setup.md && grep -q "v=spf1 include:mlsend.com" docs/email-dns-setup.md && grep -q "ml._domainkey" docs/email-dns-setup.md && grep -q "v=DMARC1" docs/email-dns-setup.md && echo "DNS_DOCS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - docs/email-dns-setup.md exists
    - File contains SPF record: `v=spf1 include:mlsend.com ~all`
    - File contains DKIM CNAME: `ml._domainkey` pointing to `ml.domainkey.mlsend.com`
    - File contains DMARC record: `v=DMARC1; p=none`
    - File contains MXToolbox verification URLs
    - File contains MailerLite setup steps
  </acceptance_criteria>
  <done>Email DNS documentation complete with exact SPF/DKIM/DMARC records, verification steps, and MailerLite setup instructions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Netlify deploy and email DNS setup</name>
  <files>none</files>
  <action>
This is a human verification checkpoint. After committing and pushing Tasks 1-2 to main, the user verifies Netlify deployment works correctly and begins email DNS configuration.
  </action>
  <read_first>
    - docs/email-dns-setup.md (the DNS documentation the user will follow)
  </read_first>
  <what-built>
    Netlify CI/CD pipeline configured with root netlify.toml (build command: npm run build, publish: dist/, Node 22).
    GitHub Pages workflows disabled (manual-only trigger).
    Email DNS records documented in docs/email-dns-setup.md.
  </what-built>
  <how-to-verify>
    **Netlify Deploy (after committing and pushing to main):**
    1. Visit the Netlify dashboard for the intelligenteconomics site
    2. Confirm the latest deploy used `npm run build` and published from `dist/`
    3. Visit the Netlify site URL — should show the Astro placeholder page ("Intelligent Economics / Build the Next World") with colored text, NOT the old index.html
    4. Create a test PR — verify a Deploy Preview URL appears as a comment or check

    **GitHub Actions:**
    5. Visit https://github.com/basicScandal/intelligent-economics/actions
    6. Confirm no GitHub Pages deploy was triggered by the push to main

    **Email DNS (action required):**
    7. Open docs/email-dns-setup.md and follow the instructions to add SPF, DKIM, and DMARC records at your domain registrar
    8. After adding records, verify at https://mxtoolbox.com/spf.aspx with intelligenteconomics.ai
  </how-to-verify>
  <verify>
    <automated>echo "CHECKPOINT: Requires human verification of Netlify deploy and DNS setup"</automated>
  </verify>
  <acceptance_criteria>
    - Netlify dashboard shows successful deploy from dist/ directory
    - Netlify site URL shows Astro placeholder page with colored text
    - No GitHub Pages workflow was triggered on push to main
    - DNS records are queued or added at domain registrar
  </acceptance_criteria>
  <done>Netlify deployment confirmed working, GitHub Pages workflows confirmed disabled, email DNS records queued or configured</done>
  <resume-signal>Type "approved" if Netlify deploys correctly and DNS records are queued/added, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
```bash
# Verify netlify.toml is correctly configured
cd /Users/scandal/ai/intelligenteconomics
grep 'publish = "dist"' netlify.toml
grep 'NODE_VERSION = "22"' netlify.toml

# Verify GitHub Pages workflows are disabled
! grep -q 'push:' .github/workflows/deploy.yml && echo "deploy.yml: push trigger removed"
! grep -q 'push:' .github/workflows/static.yml && echo "static.yml: push trigger removed"

# Verify build still works with new config
npm run build && echo "BUILD_PASS"

# Verify email DNS docs exist
test -f docs/email-dns-setup.md && echo "DNS_DOCS_EXIST"
```
</verification>

<success_criteria>
- netlify.toml at project root configures build from dist/ with Node 22 (ARCH-04)
- Both GitHub Pages auto-deploy workflows are disabled (ARCH-04)
- Netlify deploy succeeds and serves the Astro placeholder page (ARCH-04)
- PR preview deploy URLs work (ARCH-04)
- Email DNS records documented with exact values and verification steps
- manual.yml workflow is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-deploy/01-02-SUMMARY.md`
</output>
